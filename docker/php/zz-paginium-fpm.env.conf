; Pass container env (GIT_CONFIG_*, APP_*) into FPM workers — default clear_env=yes strips them,
; so exec('git …') from PHP would miss safe.directory even when compose sets GIT_CONFIG_*.
[www]
clear_env = no
