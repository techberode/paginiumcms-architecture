/**
 * Iteration 44 — shared admin list filters (search, status, SEO, page size).
 * Thin alias over AdminListToolbar for consistent naming in docs and imports.
 */
export {
  AdminListToolbar as AdminListFilterBar,
  DEFAULT_PAGE_SIZE_OPTIONS,
  type AdminListToolbarProps as AdminListFilterBarProps,
  type AdminStatusOption,
} from './AdminListToolbar';
