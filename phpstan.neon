includes:
    - vendor/phpstan/phpstan-phpunit/extension.neon

parameters:
    level: 8

    paths:
        - backend/app
        - backend/bootstrap
        - backend/tests
        - backend/bin

    scanDirectories:
        - backend/app/Support
        - backend/app/Modules

    excludePaths:
        - backend/vendor/*
        - backend/storage/*
        - backend/public/*

    phpVersion: 80500

    # Sequential workers: parallel AppImage/node workers fail on some hosts (FUSE / php.ini).
    parallel:
        maximumNumberOfProcesses: 1

    ignoreErrors:
        # Legacy routes folder removed; public debug scripts excluded above.
