# Terminal Breach (bundled theme)

Reference SOC-terminal theme for PaginiumCMS It.83e. Activate from **Admin → Themes** — no ZIP upload required.

- Dark-default palette via `terminal-breach` color scheme
- React `PublicShell` in `frontend/src/themes/terminal-breach/`
- CSP-safe CSS only (no inline scripts)
